import type { BrandConfig } from '@strategicnerds/slide-nerds'

/**
 * Brand configuration for {{PROJECT_NAME}}.
 *
 * Change these values to match your brand. The layout template reads
 * these and injects them as CSS custom properties on the <html> element,
 * so every slide automatically picks up your palette, fonts, and spacing.
 *
 * Colors are hex. The layout also derives RGB triples automatically so
 * that mesh backgrounds, accent glows, and subtle surface tints can use
 * color-mix / rgba() without you having to compute them by hand.
 *
 * Pick colors at https://coolors.co or with the macOS color picker.
 */
export default {
  colors: {
    // Deepest background; used in section gradients and slide chrome.
    primary: '#0d0d0f',
    // Your brand accent. Drives pills, timeline dots, the section label,
    // mesh tints, and the stat glow. Pick a saturated warm or cool tone.
    accent: '#f59e0b',
    // The slide canvas color behind all content.
    background: '#111114',
    // Elevated surfaces: cards, modals, tables.
    surface: '#1a1a1f',
    // Primary text color. High contrast against background.
    text: '#e8e6e3',
  },
  fonts: {
    heading: 'system-ui, -apple-system, sans-serif',
    body: 'system-ui, -apple-system, sans-serif',
    mono: 'ui-monospace, monospace',
  },
  spacing: {
    // Outer slide padding. SlideFrame uses this as its default.
    slide: '5rem',
    section: '2rem',
    element: '1rem',
  },
  // logo: {
  //   src: '/logo.svg',   // Path to your brand logo in public/
  //   height: 28,          // Height in pixels for the slide watermark
  // },
} satisfies BrandConfig
