import { ReferenceContent } from '@/components/docs/ReferenceContent'

export const metadata = { title: 'Slide definition reference' }

export default function ReferenceHelpPage() {
  return <ReferenceContent variant="dashboard" />
}
